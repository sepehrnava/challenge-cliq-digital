import Spinner from "../ui/Spinner";

// a component that shows a loading spinner
const CityLoading = () => {
  return (
    <div className="">
      <Spinner />
    </div>
  );
};

export default CityLoading;
